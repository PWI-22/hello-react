const App = () => {

    const message = <div>Hello World!</div>
    const title = <h1>Welcome to React!!</h1>

    return (
        <div>
            {title}
            {message}
        </div>
    ) 
}

export default App;
